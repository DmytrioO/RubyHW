<h1>Dear <%= @order.user.name %> thank you for your order!</h1>
<p>Our manager will connect you as soon as possible!</p>
<p>Here are some details:</p>
<h2>Order #<%= @order.id %></h2>
<p>Created at: <%= @order.created_at.strftime("%d/%m/%Y at %H:%M") %></p>
<p>Products:</p>
<ul>
    <% @order.cart.cart_products.each do |cart_product| %>
      <li>
        <p><strong>Name:</strong> <%= cart_product.name %><br></p>
        <p><strong>Price:</strong> <%= number_to_currency(cart_product.price, unit: '$', separator: ',', delimiter: ' ') %><br></p>
        <p><strong>Quantity:</strong> <%= cart_product.quantity %></p>
      </li>
    <% end %>
</ul>
<p>Total: <%= number_to_currency(@order.order_information.total, unit: '$', separator: ',', delimiter: ' ') %></p>
<p>You can see this <%= link_to 'order', @order %> on our <%= link_to 'website', products_url %></p>
<p>Have a good day!</p>